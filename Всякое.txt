python src/scripts/create_result_match_maps_table.py 
Миграция для новой таблицы


CREATE TABLE IF NOT EXISTS predict (
    match_id INTEGER PRIMARY KEY,
    team1_score REAL,
    team2_score REAL,
    team1_score_final INTEGER,
    team2_score_final INTEGER,
    confidence REAL,
    model_version TEXT,
    last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS predict_map (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    match_id INTEGER NOT NULL,
    map_name TEXT NOT NULL,
    team1_rounds REAL,
    team2_rounds REAL,
    team1_rounds_final INTEGER,
    team2_rounds_final INTEGER,
    confidence REAL,
    model_version TEXT,
    last_updated TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);